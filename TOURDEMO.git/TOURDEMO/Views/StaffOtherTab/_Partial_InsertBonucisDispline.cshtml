@using TOURDEMO.Utilities
@model CRM.Core.tbl_StaffBonusDiscipline

<div class="modal fade bs-modal-lg" id="modal-insert-bonusdiscipline" role="document" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-hidden="true"></button>
                <h4 class="modal-title">Khen thưởng/Kỉ luật</h4>
            </div>
            @using (Ajax.BeginForm("CreateBonusDiscipline", "StaffOtherTab", null, new AjaxOptions { OnFailure = "OnFailureBonusDiscipline", OnSuccess = "OnSuccessBonusDiscipline", UpdateTargetId = "bonusdiscipline" }))
            {
                <div class="modal-body">
                    <div class="form-horizontal">
                        <div class="row">
                            <div class="col-md-12">
                                <div class="form-group">
                                    <label class="control-label col-lg-2 col-md-2">Chọn type</label>
                                    <div class="col-lg-10 col-md-10">
                                        <select class="form-control" style="width: 100%" id="insert-typeID" name="TypeId">
                                            <option value="1">Khen thưởng</option>
                                            <option value="2">Kỉ luật</option>
                                        </select>

                                    </div>
                                </div>
                                <div class="form-group">
                                    <label class="control-label col-lg-2 col-md-2">Nội dung</label>
                                    <div class="col-lg-10 col-md-10">
                                        @*@Html.TextBoxFor(model => model.Name, new { @class = "form-control" })*@
                                        @Html.TextAreaFor(x => Model.Name, new { @class = "form-control", @placeholder = "Nội dung khen thưởng kỉ luật", @id = "noidungkTTL" })
                                    </div>
                                </div>
                                <div class="form-group">
                                    <label class="control-label col-lg-2 col-md-2">Ghi chú</label>
                                    <div class="col-lg-10 col-md-10">
                                        @Html.TextAreaFor(x => Model.Note, new { @class = "form-control", @placeholder = "Ghi chú", @id = "ghichukTTL" })
                                    </div>
                                </div>
                                @*<div class="form-group">
                                    <label class="control-label col-lg-2 col-md-2">Lương phụ cấp</label>
                                    <div class="col-lg-10 col-md-10">
                                        @Html.TextBoxFor(model => model.SubsidySalary, new { @class = "form-control" })
                                    </div>
                                </div>
                                <div class="form-group">
                                    <label class="control-label col-lg-2 col-md-2">Ngày áp dụng</label>
                                    <div class="col-lg-10 col-md-10">
                                        @Html.TextBoxFor(model => model.StartDate, "{0:yyyy-MM-dd}", new { @class = "form-control", @type = "date" })
                                    </div>
                                </div>*@
                            </div>
                        </div>
                    </div>
                </div>
                <div align="center" style="margin-bottom: 20px">
                    <button type="submit" class="btn blue">Lưu</button>
                    <button type="button" class="btn default" data-dismiss="modal">Đóng</button>
                </div>
            }
        </div>
    </div>
</div>
