@using CRM.Core
@model IEnumerable<tbl_MailSendingReceives>

@{
    int i = 1;
}

<table class="table table-bordered table-hover">
    <thead>
        <tr>
            <th>#</th>
            <th>Tên danh sách</th>
            <th>Diễn giải</th>
            <th>Người tạo</th>
            <th>Ngày tạo</th>
        </tr>
    </thead>
    <tbody>
        @if (Model != null)
        {
            foreach (var item in Model)
            {
                <tr class="odd gradeX" id="item-@item.Id">
                    <td><a style="cursor: pointer" onclick="removeFromList(@i, @item.tbl_MailReceives.Id, @item.Id)"><i class="fa fa-trash"></i>&nbsp;Xóa</a></td>
                    <td>@item.tbl_MailReceives.Name</td>
                    <td>@Html.Raw(item.tbl_MailReceives.Description)</td>
                    <td>@item.tbl_MailReceives.tbl_Staff.FullName</td>
                    <td>@string.Format("{0:dd/MM/yyyy}", item.tbl_MailReceives.CreateDate)</td>
                </tr>
                i++;
            }
        }
    </tbody>
</table>


