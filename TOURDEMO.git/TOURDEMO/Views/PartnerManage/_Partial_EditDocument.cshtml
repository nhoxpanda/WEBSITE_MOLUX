@using CRM.Core
@using TOURDEMO.Utilities
@model tbl_DocumentFile

<div class="modal fade " id="modal-edit-document" role="document" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-hidden="true"></button>
                <h4 class="modal-title">Cập nhật tài liệu</h4>
            </div>
            @using (Ajax.BeginForm("UpdateDocument", "PartnerManage", null, new AjaxOptions { OnFailure = "OnFailureDocument", OnSuccess = "OnSuccessDocument", UpdateTargetId = "tailieumau" }, new { enctype = "multipart/form-data" }))
            {
                <div class="modal-body">
                    <div class="form-horizontal">
                        <div class="row">
                            <div class="col-md-12">
                                <div class="form-group">
                                    <label class="control-label col-lg-3 col-md-3">Code</label>
                                    <div class="col-lg-9 col-md-9">
                                        @Html.TextBoxFor(model => model.Code, new { @class = "form-control", @readonly = "readonly" })
                                    </div>
                                </div>
                                <div class="form-group">
                                    <label class="control-label col-lg-3 col-md-3">File</label>
                                    <div class="col-lg-9 col-md-9">
                                        @Html.HiddenFor(model => model.Id)
                                        @Html.HiddenFor(model => model.PartnerId)
                                        @Html.HiddenFor(model => model.CreatedDate)
                                        @Html.HiddenFor(model => model.PermissionStaff)
                                        @Html.HiddenFor(model => model.FileName)
                                        @Html.HiddenFor(model => model.FileSize)
                                        @Html.HiddenFor(model => model.StaffId)

                                        @Html.TextBoxFor(model => model.FileName, new { type = "file", @class = "form-control" })
                                    </div>
                                </div>
                                <div class="form-group">
                                    <label class="control-label col-lg-3 col-md-3">Loại tài liệu</label>
                                    <div class="col-lg-9 col-md-9">
                                        @Html.DropDownList("DictionaryId", null, new { @class = "form-control", id = "edit-document-type", style = "width: 100%" })
                                    </div>
                                </div>
                                <div class="form-group">
                                    <label class="control-label col-lg-3 col-md-3">Tags</label>
                                    <div class="col-lg-9 col-md-9">
                                        <select class="form-control" name="TagsId" id="edit-tag-document" style="width: 100%" multiple="multiple">
                                            @foreach (var item in LoadData.DropdownlistLocation())
                                            {
                                                <option value="@item.Id" @(Model.TagsId != null && Model.TagsId.Contains(item.Id.ToString()) ? "selected" : "")>@item.Tags</option>
                                            }
                                        </select>
                                    </div>
                                </div>
                                <div class="form-group">
                                    <label class="control-label col-lg-3 col-md-3">Diễn giải</label>
                                    <div class="col-lg-9 col-md-9">
                                        @Html.TextAreaFor(model => model.Note, new { @class = "form-control", id = "edit-document-note" })
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                <div align="center" style="margin-bottom: 20px">
                    <button type="submit" class="btn blue" id="btnLuuThem">Lưu</button>
                    <button type="button" class="btn default" data-dismiss="modal">Đóng</button>
                </div>
            }
        </div>
        <!-- /.modal-content -->
    </div>
    <!-- /.modal-dialog -->
</div>

